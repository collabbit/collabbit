<%
welcome = "Please log in to the #{@instance.short_name} collabbit"
path = session_path
%>

<%= render :partial => 'new', :locals => {  :welcome => welcome,
                                            :path => path,
                                            :show_remember_me => true,
                                            :show_account_links => true} %>