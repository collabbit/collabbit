<% content_for :title, title ('Edit update', @update.title, 'Update', @incident.name, 'Incidents') %>
<% content_for :javascripts, javascript_include_tag('jquery', 'jquery.multifile', 'updates') -%>

<% content_for :breadcrumbs, 
	breadcrumbs(['Incidents',incidents_path],
				[@incident.name,incident_path(@incident)],
				[@update.title,incident_update_path(@incident,@update)]) %>
<% content_for :subheader_title, 'Edit Update' %>

<% content_for :body do %>
   	<%= render :partial => 'form', :locals => {:button_text => 'Save Changes'} %>
<% end %>
