<% content_for :title, title ('Create an update', @incident.name, 'Incidents') %>
<% content_for :javascripts, javascript_include_tag('jquery', 'jquery.multifile', 'updates') -%>

<% content_for :breadcrumbs, 
	breadcrumbs(['Incidents',incidents_path],
				[@incident.name,incident_path(@incident)]) %>
<% content_for :subheader_title, 'Add Update' %>

<% content_for :body do %>
    <%= render :partial => 'form', :locals => {:button_text => 'Create Update'} %>
<% end %>
